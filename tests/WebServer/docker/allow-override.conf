# Allow .htaccess overrides in the document root so the web-server MCP test can toggle
# .htaccess behaviour at runtime (mod_php variant; the Debian default is AllowOverride None).
<Directory /var/www/html>
	AllowOverride All
</Directory>
